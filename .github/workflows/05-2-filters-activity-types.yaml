name: 05-2- Events Filters and Activity Types

on:

    pull_request:
        types: closed
        branches:
            - main

jobs:
    filters-activity-types:
        runs-on: ubuntu-latest
        steps:
        - run: echo " Running whenever a PR is closed on the main branch"